{
	"name": "@mindclade/internal-console-client",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "rm -rf dist/native && tsc -p tsconfig.json --noEmit false --rootDir ../.. --outDir dist/native",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "pnpm --filter @mindclade/internal-sdk build && pnpm run build && node --test dist/native/apps/console/tests/*.test.js",
		"lint": "cd ../.. && biome check --config-path apps/console/biome.json apps/console"
	},
	"dependencies": {
		"@mindclade/internal-sdk": "workspace:*"
	},
	"devDependencies": {
		"@bufbuild/protobuf": "2.11.0",
		"@connectrpc/connect": "catalog:",
		"@types/node": "catalog:",
		"typescript": "catalog:"
	}
}
