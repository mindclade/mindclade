name: OpenSSF Scorecard

on:
  push:
    branches: [main]
  schedule:
    - cron: "31 6 * * 3"

permissions:
  contents: read

concurrency:
  group: ${{ github.repository }}-scorecard-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  scorecard:
    permissions:
      contents: read
      security-events: write
    uses: mindclade/.github/.github/workflows/reusable-scorecard.yml@fc5af9efc19b47078fe446feee750d7f4973195b
    with:
      source_revision: ${{ github.sha }}
