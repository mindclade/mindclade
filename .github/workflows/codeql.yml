name: CodeQL

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "17 5 * * 2"

permissions:
  contents: read

concurrency:
  group: ${{ github.repository }}-codeql-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  python:
    permissions:
      contents: read
      security-events: write
    uses: mindclade/.github/.github/workflows/reusable-codeql.yml@55687ae97dbf6073d61a085c21542637a551ed00
    with:
      source_revision: ${{ github.event.pull_request.head.sha || github.sha }}
      languages: '["python"]'
      build_mode: none
