name: CodeQL

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "17 5 * * 2"

permissions:
  contents: read

concurrency:
  group: ${{ github.repository }}-codeql-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  python:
    uses: mindclade/.github/.github/workflows/reusable-codeql.yml@6399abc50c4678d0dff7f33bbd7f6868043ef736
    with:
      source_revision: ${{ github.event.pull_request.head.sha || github.sha }}
      languages: '["python"]'
      build_mode: none
