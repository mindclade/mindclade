# License Inventory

`license_inventory.json` is the checked-in inventory for material physically
vendored under `third_party`. It is intentionally empty while dependencies are
resolved only through their pinned native package authorities. A vendored
entry must identify its immutable source digest, SPDX license expression, and
notice location before review.
