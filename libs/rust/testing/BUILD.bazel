load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(name = "testing", crate_name = "mindclade_testing", edition = "2024", srcs = glob(["src/**/*.rs"]), deps = ["//libs/rust/retry:retry"], visibility = ["//visibility:public"])

filegroup(
    name = "governed_sources",
    srcs = ["BUILD.bazel"] + glob(["**"], exclude = ["BUILD.bazel"]),
    visibility = ["//visibility:public"],
)
