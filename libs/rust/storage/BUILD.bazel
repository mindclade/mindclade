load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(name = "storage", crate_name = "mindclade_storage", edition = "2024", srcs = glob(["src/**/*.rs"]), deps = ["//libs/rust/artifact:artifact"], visibility = ["//visibility:public"])

filegroup(
    name = "governed_sources",
    srcs = ["BUILD.bazel"] + glob(["**"], exclude = ["BUILD.bazel"]),
    visibility = ["//visibility:public"],
)
