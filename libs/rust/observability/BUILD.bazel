load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(name = "observability", crate_name = "mindclade_observability", edition = "2024", srcs = glob(["src/**/*.rs"]), visibility = ["//visibility:public"])

filegroup(
    name = "governed_sources",
    srcs = ["BUILD.bazel"] + glob(["**"], exclude = ["BUILD.bazel"]),
    visibility = ["//visibility:public"],
)
