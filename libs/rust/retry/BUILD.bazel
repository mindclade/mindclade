load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "retry",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "mindclade_retry",
    edition = "2024",
    visibility = ["//visibility:public"],
)

filegroup(
    name = "governed_sources",
    srcs = ["BUILD.bazel"] + glob(
        ["**"],
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
