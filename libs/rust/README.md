# Rust Foundation Crates

Wave 1 Rust foundations are small, independently buildable crates. They carry
typed values and deterministic local behavior only; no crate connects to a
cloud, registry, or deployment authority.
